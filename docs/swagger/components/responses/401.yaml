description: Unauthorized Invalid or missing token
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
      properties:
        status:
          type: integer
          example: 401
        message:
          type: string
          example: 'Unauthorized: Invalid or missing token'
